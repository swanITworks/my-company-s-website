import React from "react"
import Layout from "../hoc/Layout/layout"
import Slider from "../components/02Slider/slider"

const about = () => (
  <Layout>
    <Slider selected={1}/>
    <p>This site not exist.</p>
  </Layout>
)

export default about;
